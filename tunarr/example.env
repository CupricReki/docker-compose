# Copy to /opt/tunarr/.env and adjust. Defaults match docker-compose.yml (${VAR:-value});
# uncomment to override. Do not duplicate globals (TZ, ROOT_DIR) — dc() sources those.

# TUNARR_PORT=8000
# TUNARR_TAG=latest
# TUNARR_LOG_LEVEL=INFO
