export declare class TargetValueTimer<T> {
    private timeout;
    private targetValue;
    setTarget(value: T, duration: number): void;
    hasTarget(): boolean;
    getTarget(): T;
    reset(): void;
}
