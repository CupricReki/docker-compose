import { RingPlatformConfig } from './config';
import { RingCamera } from '../api';
import { BaseDataAccessory } from './base-data-accessory';
import { Logging, PlatformAccessory } from 'homebridge';
export declare class Camera extends BaseDataAccessory<RingCamera> {
    readonly device: RingCamera;
    readonly accessory: PlatformAccessory;
    readonly logger: Logging;
    readonly config: RingPlatformConfig;
    private inHomeDoorbellStatus;
    private cameraSource;
    constructor(device: RingCamera, accessory: PlatformAccessory, logger: Logging, config: RingPlatformConfig);
    private loadSnapshotForEvent;
}
