import { API, DynamicPlatformPlugin, Logging, PlatformAccessory, PlatformConfig } from 'homebridge';
import { RingPlatformConfig } from './config';
import { RefreshTokenAuth } from '../api/rest-client';
export declare const platformName = "Ring";
export declare const pluginName = "homebridge-ring";
export declare class RingPlatform implements DynamicPlatformPlugin {
    log: Logging;
    config: PlatformConfig & RingPlatformConfig & RefreshTokenAuth;
    api: API;
    private readonly homebridgeAccessories;
    constructor(log: Logging, config: PlatformConfig & RingPlatformConfig & RefreshTokenAuth, api: API);
    configureAccessory(accessory: PlatformAccessory): void;
    connectToApi(): Promise<void>;
}
