---
version: "2.1"
services:
  readarr:
    image: ghcr.io/linuxserver/readarr:latest
    container_name: readarr
    environment:
      - PUID=1007
      - PGID=1008
      - TZ=America/Denver
      - DOCKER_MODS=ghcr.io/gilbn/theme.park:radarr
      - TP_THEME=hotline
    volumes:
      - /opt/radarr/config:/config
      - /mnt/misc/books:/books #optional
      - /mnt/downloads:/downloads #optional
    ports:
      - 8787:8787
    restart: unless-stopped
